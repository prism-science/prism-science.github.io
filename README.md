# prism-science.github.io

This repo now only anchors the `diffuse.science` domain so that the
DiffUSE Logbook (repo `prism-science/logbook`) is served at
https://diffuse.science/logbook/. The root URL redirects there.

**Do not delete `CNAME` or turn off GitHub Pages for this repo.**
Doing so moves the logbook off `diffuse.science`.

## Where the old site went
- Full former website: tag `diffuse-site-final-2026-09`
  (also branch `archive/diffuse-site-2026-09`)
- Unmerged blog post from PR #66: tag `kl-blog-post-2026-09`

To browse the old site files on GitHub, pick the tag from the branch/tag dropdown.
